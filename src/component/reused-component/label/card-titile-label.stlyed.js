import styled from 'styled-components'
export const CardLabelStyle = styled.h3
`
  margin-bottom: 1em;
  margin-top: 0.85em;
  
  font-size: clamp(1.25em,1.5vw,2em);
  
  text-transform: capitalize;
  font-family: var(--font-manrope);
  font-weight: 800;
`