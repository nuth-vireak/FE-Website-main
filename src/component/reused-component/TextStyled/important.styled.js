import styled from "styled-components";

export const ImportantTextStyled = styled.p
    `
      white-space: break-spaces;
      margin-top: 4em;
      font-size: clamp(.6em,1.2vw,.85em);
      text-align: center;
      color: #DEDEDE;
      letter-spacing: 0.015em;
      font-style: italic;
      font-weight: bold;
      font-family: var(--font-lato);
      text-transform: uppercase;
    `